---
const props = Astro.props;
const { src, alt, classes } = props;
---

<img
  src={src}
  alt={alt}
  loading="lazy"
  class="jm-image-logo"
  class:list={classes}
  width="48"
  height="48"
/>

<style>
  .jm-image-logo {
    @apply h-12 w-12 rounded-full border border-gray-400;
  }
</style>
