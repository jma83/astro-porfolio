---
const props = Astro.props;
const { src, alt, classes } = props;
---

<img
  src={src}
  alt={alt}
  loading="lazy"
  class="jm-image-logo"
  class:list={classes}
/>

<style>
  .jm-image-logo {
    @apply w-12 h-12 rounded-full border border-gray-400;
  }
</style>
