<ul class="scroll-container">
  <slot />
</ul>

<style>
  .scroll-container {
    @apply flex flex-row flex-nowrap w-auto items-center pt-8 pb-4 gap-4;
    @apply whitespace-nowrap overflow-x-scroll;
  }

  ::-webkit-scrollbar {
    @apply hidden h-0 w-0;
  }

  ::-webkit-scrollbar-thumb {
    @apply hidden;
  }

  @screen md {
    .scroll-container {
      @apply flex-wrap;
      overflow-x: unset;
    }
  }
</style>
